module.exports = {
  project: {
    android: {
      sourceDir: './android',
    },
  },
  assets: ['./src/assets/fonts/'],
  dependencies: {
    // react-native-vector-icons families are linked via the new
    // @react-native-vector-icons scoped packages; no manual config needed.
  },
};
